# Deprecation and removal warnings this build is known to emit, checked by the shared CI's
# deprecation-baseline action. A warning not listed here fails the build.
#
# Fix a warning rather than adding to this file wherever the code is ours. Everything below comes
# from a dependency, so the only ways out are the dependency moving or us dropping it.
#
# RocksDB, pulled in by kafka-streams for its state stores. It loads a native library, which is
# restricted access the JVM will block outright rather than warn about in a future release. The
# fix is --enable-native-access for the module, which kafka-streams has to apply to itself; the
# alternative is passing ALL-UNNAMED here, which would silence every such warning in the build
# including ones nothing has looked at yet.
WARNING: A restricted method in java.lang.System has been called
WARNING: Restricted methods will be blocked in a future release unless native access is enabled
WARNING: Use --enable-native-access=ALL-UNNAMED to avoid a warning for callers in this module
WARNING: java.lang.System::loadLibrary has been called by org.rocksdb.RocksDB in an unnamed module
